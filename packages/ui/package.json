{
  "name": "@meadowmark/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Meadowmark DOM interface layer: Material Design 3 HUD, panels and shared UI systems rendered over the three.js world.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
